# 多层级分类系统改造讲解

> 本文档详细记录了如何将博客的分类系统从"两级"改造为"支持任意多层级"。从项目架构总览、文件职责划分，到每一行代码的逐行讲解，兼顾基础概念。

---

## 一、总述：改造目标与涉及范围

### 1.1 改造目标

原来博客的分类只支持两级，例如：
- `"编程"` —— 顶级分类
- `"编程/算法"` —— 顶级下的子分类

中间的层级会被忽略，只取首尾两段。改造后支持**任意多层级**，例如：
- `"编程/前端/框架/React"` —— 三级
- `"生活/旅行/2024/日本"` —— 四级

### 1.2 改造涉及的文件

| 文件路径 | 作用 | 是否新建 |
|---|---|---|
| `src/utils/content-utils.ts` | 数据层：把文章分类路径解析成树形结构 | 修改 |
| `src/components/widget/Categories.astro` | 侧边栏渲染：显示分类树 | 修改 |
| `src/components/CascadeSelector.svelte` | 导航栏级联选择器：hover 时展开多层级面板 | 修改 |
| `src/components/Navbar.astro` | 导航栏：把分类树数据转成级联选择器格式 | 修改 |
| `src/types/config.ts` | 类型定义：定义分类节点的数据结构 | 查看 |
| `src/config.ts` | 配置：定义导航栏显示哪些链接 | 查看 |

### 1.3 分类数据的流转分两层：**数据层（content-utils.ts）**负责把文章路径解析成树，**展示层（Astro/Svelte 组件）**负责把树渲染成界面。

```
文章 frontmatter
    category: "编程/前端/React"
        │
        ▼
┌─────────────────────────────────────────────────────────┐
│  content-utils.ts                                        │
│                                                          │
│  getCategoryTree()                                       │
│      │                                                   │
│      └── buildCategoryTree()   ← 把所有文章的分类路径    │
│                                   解析成树形结构          │
│                                   返回 CategoryNode[]    │
└─────────────────────────────────────────────────────────┘
        │
        │  多个组件各自调用 getCategoryTree() 获取数据
        │
        ├──→ Navbar.astro
        │       │  await getCategoryTree()
        │       │  convertToCascaderOptions() 转成级联选择器格式
        │       ▼
        │       CascadeSelector.svelte 渲染导航栏下拉面板
        │
        └──→ Categories.astro（侧边栏小部件）
                await getCategoryTree()
                renderCategoryNodes() 递归渲染 HTML 字符串
                set:html 注入到页面显示侧边栏
```

**注意：归档页 `archive.astro` 用的是 `getSortedPostsList()`（按时间排序的文章列表），跟分类树没有关系。**

---

## 二、文件职责详解

### 2.1 类型定义文件 `src/types/config.ts`

这个文件**定义数据结构**（类似其他语言的"类"或"接口"），但不赋值。它告诉 TypeScript 编译器："我需要的数据长什么样"。

```typescript
// 第 37-43 行：定义了一个"枚举"，表示导航栏的预设链接类型
export enum LinkPreset {
    Home = 0,      // 首页
    Archive = 1,   // 归档页
    About = 2,     // 关于页
    Category = 3,   // 分类页（← 本次改造重点相关）
    Tags = 4,      // 标签页
}
```

> **枚举 enum 是什么？** 枚举是一种把数字和名字对应起来的方式。`LinkPreset.Category = 3` 意味着在代码里写 `LinkPreset.Category` 就等于写 `3`。这样比直接写数字更易读，也方便以后修改。

### 2.2 配置值文件 `src/config.ts`

这个文件是**实际使用的配置值**，引用了 `types/config.ts` 中定义的类型。

```typescript
// 第 76-90 行：导航栏配置
export const navBarConfig: NavBarConfig = {
    links: [
        LinkPreset.Home,       // ← 引用枚举，值为数字 0
        LinkPreset.Archive,
        LinkPreset.About,
        LinkPreset.Category,   // ← 当识别到这个值时，Navbar.astro 会渲染 CascadeSelector
        LinkPreset.Tags,
    ],
};
```

### 2.3 核心数据文件 `src/utils/content-utils.ts`

这是**最重要**的文件。它是"数据中枢"，负责：
1. 从 Astro 的内容集合（collection）中获取所有文章
2. 统计每个分类有多少篇文章
3. 把分类路径字符串（`"编程/前端/React"`）构建成树形结构

---

## 三、修改内容逐行详解

### 3.1 `src/utils/content-utils.ts` — 分类树构建

#### 3.1.1 节点数据结构定义

```typescript
// 第 73-79 行：分类树节点的 TypeScript 类型定义
export type CategoryNode = {
    name: string;           // 节点名称，如 "编程"
    count: number;          // 该分类下的文章总数
    url: string;            // 点击后跳转的 URL
    children?: CategoryNode[];  // 可选字段：子节点数组
};
```

> **`type` 定义中的 `?` 是什么意思？**
> `children?: CategoryNode[]` 的 `?` 表示这个字段**可能存在也可能不存在**。存在时类型是 `CategoryNode[]`（子节点数组），不存在时是 `undefined`。
>
> 这样设计是因为：叶子节点（最底层的分类）没有子节点，所以不需要 `children` 字段。

#### 3.1.2 本次改造核心算法：buildCategoryTree

#### 递归插入算法

改造思路：**不预先假设层级数量，而是逐级往下走，每遇到一段就往树的下一层插入。**



##### 输入数据

`buildCategoryTree` 接收的文章数组（从 `src/content/posts/` 扫描得到）：

```javascript
posts = [
    { data: { category: "Examples" } },           // expressive-code.md
    { data: { category: "前端/CSS" } },          // temp-frontend-css-demo.md
    { data: { category: "业务/水文" } },         // temp-business-water-post.md
]
```

---

##### 第一阶段：统计叶子节点（`leafCounts`）

遍历三篇文章，把 `category` 作为 key 累加计数：

**阶段一结果：**

```javascript
leafCounts = {
    "Examples":    1,
    "前端/CSS":    1,
    "业务/水文":   1
}
```

这是一个"字典"（key-value 对），key 是完整路径，value 是该路径下的文章数量。

---

##### 第二阶段：逐条路径构建树

对 `leafCounts` 里的每一条路径，按 `/` 拆分后逐级插入到树中。

**处理第1条：`"Examples"`（单级路径）**

`parseCategoryPath("Examples")` → `["Examples"]`，parts 长度为 1。

```
root = []

root 里找不到 name === "Examples" 的节点，
→ 新建根节点，push 进去

root = [
    { name: "Examples", count: 1, url: "/archive/?category=Examples" }
]
```

**处理第2条：`"前端/CSS"`（两级路径）**

`parseCategoryPath("前端/CSS")` → `["前端", "CSS"]`，parts 长度为 2。

**第1步**：在 root 里找 `name === "前端"` → 找不到，新建根节点：

```
root = [
    { name: "Examples", count: 1, url: "..." },
    { name: "前端", count: 0, url: "/archive/?category=前端" }   // ← 新建
]
```

**第2步**：调用 `insertNodeRecursive`，往 `"前端"` 下面插子节点。

```
调用 insertNodeRecursive(node="前端", parts=["CSS"], fullPath="前端/CSS", count=1, parentPath="前端")

    parts = ["CSS"]，解构得 currentName = "CSS"，rest = []

    root["前端"] 没有 children → 创建空数组 []
    在 children 里找 name === "CSS" → 找不到
    rest 为空，是叶子节点 → 直接 push

root["前端"].children = [
    { name: "CSS", count: 1, url: "/archive/?category=前端/CSS" }
]
```

**阶段二中途状态：**

```
root = [
    { name: "Examples", count: 1, url: "..." },
    {
        name: "前端",
        count: 0,
        url: "/archive/?category=前端",
        children: [
            { name: "CSS", count: 1, url: "/archive/?category=前端/CSS" }
        ]
    }
]
```

---

##### 第三阶段：计算父节点总数

遍历所有节点，把 count 累加到父节点：

```
对 "Examples"：没有 children，不变，count = 1

对 "前端"：有 children
    → 先递归处理 "CSS"，无 children，不变
    → "前端".count += "CSS".count = 0 + 1 = 1

对 "业务"：有 children
    → 先递归处理 "水文"，无 children，不变
    → "业务".count += "水文".count = 0 + 1 = 1
```

---

##### 最终结果

```javascript
[
    {
        name: "Examples",
        count: 1,
        url: "/archive/?category=Examples",
        children: undefined
    },
    {
        name: "前端",
        count: 1,          // ← 1 = 0(自身) + 1("CSS")
        url: "/archive/?category=前端",
        children: [
            {
                name: "CSS",
                count: 1,
                url: "/archive/?category=前端/CSS",
                children: undefined
            }
        ]
    },
    {
        name: "业务",
        count: 1,          // ← 1 = 0(自身) + 1("水文")
        url: "/archive/?category=业务",
        children: [
            {
                name: "水文",
                count: 1,
                url: "/archive/?category=业务/水文",
                children: undefined
            }
        ]
    }
]
```

---

##### 完整变换过程汇总

```
输入：posts 数组（三篇 frontmatter 中的 category）
      │
      ▼
leafCounts 字典：{ "Examples": 1, "前端/CSS": 1, "业务/水文": 1 }
      │
      ▼
逐条路径插入 root 数组：
  "Examples"     → 直接 push 进 root（单级）
  "前端/CSS"      → root 里建"前端"，再往它的 children 里 push "CSS"（多级）
  "业务/水文"     → root 里建"业务"，再往它的 children 里 push "水文"（多级）
      │
      ▼
递归计算父节点 count = 自身 + 所有子节点 count 之和
      │
      ▼
输出：CategoryNode[] 树形结构
```

**关键点**：同一个父节点下的多篇文章会合并计数。比如 `前端/CSS` 下有 3 篇文章，那 `"前端"` 的 `count` 会是 3，`"CSS"` 的 `count` 也是 3。这是代码里 `node.count += count` 这行干的事。

```typescript
// 第 87-202 行：改造后的完整算法
function buildCategoryTree(
    posts: { data: { category: string | null } }[]
): CategoryNode[] {
```

> **函数参数类型解读：**
> `posts: { data: { category: string | null } }[]`
> - `posts` 是一个**数组**（最后的 `[]`）
> - 数组里每个元素的类型是 `{ data: { category: string | null } }`
> - `string | null` 表示类型可以是字符串，也可以是 null



> **`forEach` 和 `map` 的区别：**
> - `posts.forEach(fn)`：对每个元素执行函数，**不返回值**
> - `posts.map(fn)`：**返回一个新数组**，每个元素是函数返回值
> - 这里只需要"遍历处理"，不需要返回值，所以用 `forEach`



> **什么是"递归"？**
> 递归就是函数调用自己。想象你有一个人要爬一棵树，他爬到第一个分叉口，记录名字，然后继续往下爬到下一个分叉口，再记录名字...直到爬到叶子。
>
> `insertNodeRecursive` 做的事情就是：传入一个节点和剩余路径，函数把第一段插进去，然后**调用自己**处理剩余的路径段。



> **`find` vs `filter` 的区别：**
> - `arr.filter(fn)`：返回**所有**让函数返回 true 的元素组成的数组
> - `arr.find(fn)`：返回**第一个**让函数返回 true 的元素，只有一个

```typescript
        if (rest.length === 0) {
            // 如果 rest 是空的，说明这是最后一段，是叶子节点
            if (!node) {
                // 如果没有找到同名节点，就创建一个新的叶子节点
                parent.children.push({
                    name: currentName,
                    count: count,
                    url: getCategoryUrl(currentFullPath),
                    children: undefined,  // 叶子节点没有子节点，设为 undefined
                });
            } else {
                // 如果已经存在同名节点（同一分类路径出现多次），累加计数
                node.count += count;
                node.url = getCategoryUrl(currentFullPath);
            }
        } else {
            // rest 不为空，说明还有子路径，继续往下递归
            if (!node) {
                // 如果没有找到同名节点，创建一个新的中间节点（还不是叶子）
                node = {
                    name: currentName,
                    count: 0,  // 中间节点的 count 一开始是 0，后面由 computeParentCount 计算
                    url: getCategoryUrl(currentFullPath),  // 关键：这里用累积的完整路径！
                };
                parent.children.push(node);
            }
            // 递归调用：把剩余路径继续往下插
            // 传入 currentFullPath 作为新的 parentPath
            insertNodeRecursive(node, rest, fullPath, count, currentFullPath);
        }
    }
```

> **`!node` vs `node === undefined` 的区别：**
> - `node === undefined` 是严格比较，只有值是 `undefined` 才为 true
> - `!node` 是隐式转换，只要值是 falsy（`undefined`、`null`、`0`、`""`、`false`）就为 true
> - 但 `find()` 找不到时返回 `undefined`，所以两者在这里效果相同
> - 代码里两种写法都会出现，都是正确的

##### 遍历所有叶子路径

```typescript
    // 第 147-180 行：把每个分类路径都插入到树中
    Object.keys(leafCounts).forEach(fullPath => {
        // Object.keys(leafCounts) 返回所有 key 组成的数组
        // 如 ["编程", "编程/前端", "编程/前端/React"]

        const parts = parseCategoryPath(fullPath);
        // 把路径字符串解析成数组

        if (parts.length === 0) return;
        // 空字符串路径直接跳过

        let node = root.find(n => n.name === parts[0]);
        // 在根节点数组里查找第一段（如 "编程"）是否存在

        if (parts.length === 1) {
            // 单级分类，如 "编程"
            if (!node) {
                root.push({
                    name: parts[0],
                    count: leafCounts[fullPath] || 0,
                    url: getCategoryUrl(fullPath),
                });
            } else {
                node.count += leafCounts[fullPath] || 0;
                node.url = getCategoryUrl(fullPath);
            }
        } else {
            // 多级分类，如 "编程/前端/React"
            if (!node) {
                // 如果根节点不存在，先创建根节点
                node = {
                    name: parts[0],
                    count: 0,
                    url: getCategoryUrl(parts[0]),
                };
                root.push(node);
            }
            // 递归插入剩余路径
            // 关键：传入 parts[0] 作为 parentPath
            insertNodeRecursive(
                node,
                parts.slice(1),       // 去掉第一段，剩余 ["前端", "React"]
                fullPath,
                leafCounts[fullPath] || 0,
                parts[0]              // "编程" 作为累积路径的起点
            );
        }
    });
```

> **`.slice(1)` 的作用：**
> `.slice(start)` 是数组方法，从 start 位置截取到末尾，返回新数组。
> ```typescript
> ["编程", "前端", "React"].slice(1)  // → ["前端", "React"]
> ["编程", "前端", "React"].slice(2)  // → ["React"]
> ["编程", "前端", "React"].slice(0)  // → ["编程", "前端", "React"]（拷贝副本）
> ```

##### 计算父节点总数

```typescript
    // 第 182-189 行：计算每个父节点的文章总数
    function computeParentCount(node: CategoryNode) {
        // 用后序遍历：先处理所有子节点，再处理自己
        if (node.children && node.children.length > 0) {
            node.children.forEach(computeParentCount);
            // 递归先处理子节点

            node.count += node.children.reduce((sum, child) => sum + child.count, 0);
            // .reduce() 是数组方法，把数组"归约"成一个值
            // 从 0 开始，把每个 child.count 加起来
        }
    }
    root.forEach(computeParentCount);
```

> **`.reduce((累加器, 当前元素) => 累加器 + 当前元素, 初始值)` 详解：**
> ```typescript
> [1, 2, 3].reduce((sum, n) => sum + n, 0)
> // 第一次：sum=0, n=1 → 返回 1
> // 第二次：sum=1, n=2 → 返回 3
> // 第三次：sum=3, n=3 → 返回 6
> // 最终结果：6
> ```
>
> 这段代码把 `children` 数组里所有 `count` 加起来，累加到父节点的 `count` 上。

##### 排序

```typescript
    // 第 191-200 行：对所有层级的节点按字母排序
    function sortNodes(nodes: CategoryNode[]) {
        nodes.sort((a, b) => a.name.toLowerCase().localeCompare(b.name.toLowerCase()));
        // .sort() 是数组方法，会直接修改原数组
        // 传入比较函数：(a, b) => 返回负数 a 在前，返回正数 b 在前
        // .toLowerCase() 转小写，避免 "Apple" 排在 "banana" 前面
        // .localeCompare() 按语言习惯比较字符串（考虑中文、特殊字符等）

        nodes.forEach(node => {
            if (node.children && node.children.length > 0) {
                sortNodes(node.children);  // 递归排序子节点
            }
        });
    }
    sortNodes(root);
```

> **为什么排序要用 `.localeCompare` 而不是直接 `a.name < b.name`？**
> - `a.name < b.name` 用的是 Unicode 码点比较，中文会按 Unicode 顺序排
> - `.localeCompare()` 会根据当前语言环境排序，中文会按拼音顺序
> - 对于中文分类名（如"编程"、"生活"），localeCompare 会按拼音首字母排序

##### 改造后数据的完整示例

假设有三篇文章，分类分别是：
- `"编程"`（1篇）
- `"编程/前端"`（2篇）
- `"编程/前端/React"`（1篇）

最终生成的树形结构：

```json
[
  {
    "name": "编程",
    "count": 4,
    "url": "/archive/?category=编程",
    "children": [
      {
        "name": "前端",
        "count": 3,
        "url": "/archive/?category=编程/前端",
        "children": [
          {
            "name": "React",
            "count": 1,
            "url": "/archive/?category=编程/前端/React",
            "children": undefined
          }
        ]
      }
    ]
  }
]
```

**关键观察：**
- 每个中间节点（"前端"）的 URL 是累积的完整路径 `编程/前端`，而不是孤零零的 `前端`
- 父节点的 count（4）等于自身（0） + 所有后代之和（3 = 2+1）
- 叶子节点（"React"）的 `children` 是 `undefined` 而不是空数组 `[]`



### 3.2 `src/components/widget/Categories.astro` — 侧边栏分类组件

#### 3.2.1 文件职责

这个文件负责渲染博客侧边栏中的"分类"小部件。原来只能渲染两级，改造后支持任意多层级递归渲染。

#### 3.2.2 递归计数函数（改造后）

```typescript
// 递归计算分类总数（用于判断是否折叠）
function countAllCategories(nodes: CategoryNode[]): number {
    let count = 0;
    nodes.forEach(node => {
        count += 1;  // 把自己算进去
        if (node.children && node.children.length > 0) {
            count += countAllCategories(node.children);  // 递归统计子节点
        }
    });
    return count;
}
```

> **递归在这里的作用：**
> 计算整个树有多少个节点。无论有多少层，都递归把所有层级的节点数加起来。

#### 3.2.3 递归渲染函数（核心改造）

```typescript

// indent 是缩进深度，初始为 0，每深入一层加 1
function renderCategoryNodes(nodes: CategoryNode[], indent: number = 0): string {
    // .map() 遍历每个节点，生成对应的 HTML 片段，然后用 .join('') 拼接成一个大字符串
    return nodes.map(node => {
        const paddingLeft = `${0.75 + indent * 1.25}rem`;
        // 缩进计算：
        //   第 0 层：paddingLeft = 0.75 + 0 * 1.25 = 0.75rem
        //   第 1 层：paddingLeft = 0.75 + 1 * 1.25 = 2.0rem
        //   第 2 层：paddingLeft = 0.75 + 2 * 1.25 = 3.25rem

        const hasChildren = node.children && node.children.length > 0;
        // 有子节点 → 用 <details> 折叠显示
        // 无子节点 → 用普通 <a> 链接

        if (hasChildren) {
            // 有子分类：用 <details> 折叠元素
            return `
                <details class="category-parent" open>
                    <summary class="category-summary" style="padding-left: ${paddingLeft}">
                        <a href="${node.url}" class="category-link">${node.name.trim()}</a>
                        <span class="category-count">${node.count}</span>
                    </summary>
                    <div class="category-children">
                        ${renderCategoryNodes(node.children, indent + 1)}
                        <!-- ↑ 递归调用！indent + 1 递进到下一层 -->
                    </div>
                </details>
            `;
        } else {
            // 叶子节点：普通链接样式
            return `
                <a href="${node.url}" class="category-leaf" style="padding-left: ${paddingLeft}">
                    <span class="leaf-name">${node.name.trim()}</span>
                    <span class="category-count">${node.count}</span>
                </a>
            `;
        }
    }).join('');
    // .map() 返回数组，.join('') 把数组合并成字符串
}
```

> **`${变量}` 在模板字符串中的作用：**
> ```typescript
> const name = "小明";
> const html = `<span>${name}</span>`;  // "<span>小明</span>"
> ```
> 模板字符串里面可以嵌入任何 JS 表达式，如 `${paddingLeft}`、`${node.count}`、`${renderCategoryNodes(...)}`

> **为什么用 `set:html` 而不是直接 `<Component>`？**
> `renderCategoryNodes` 返回的是一个**字符串**（HTML 代码文本），不是真实的 DOM 元素。
> `set:html` 是 Astro 的特殊指令，作用是："把这个字符串作为 HTML 代码直接插入到 DOM 里"。

#### 3.2.4 渲染结果注入

```astro
// 第 71 行
<div class="category-tree" set:html={categoryTreeHtml}></div>
```

```typescript
// 第 58 行
const categoryTreeHtml = renderCategoryNodes(categoryTree);
```

### 3.3 `src/components/CascadeSelector.svelte` — 导航栏级联选择器

#### 3.3.1 文件职责

当用户鼠标悬停在导航栏的 "Categories" 按钮上时，展开一个横向排列的多列面板，支持鼠标 hover 逐层展开。改造后列数可以无限多。

#### 3.3.2 数据结构

```svelte
// 第 3-7 行：级联选择器使用的数据格式
interface CategoryNode {
    value: string;       // 完整路径，如 "编程/前端/React"
    label: string;      // 显示文字，如 "React"
    children?: CategoryNode[];  // 子选项
}
```

> **`interface` vs `type` 的区别（TypeScript）：**
> 功能和语法略有不同，但基本可以互换：
> - `type` 更灵活，可以做联合类型、映射类型等高级操作
> - `interface` 更声明式，适合定义对象结构
> 这个文件用 `interface`，`content-utils.ts` 用 `type`，没有本质区别

#### 3.3.3 核心派生状态

```svelte
// 第 23-33 行：用 $derived.by 定义派生状态（响应式自动计算）
let menus = $derived.by(() => {
    let opts = options;      // opts 是当前列的数据
    const result = [opts];   // result 收集所有列，第一列是根级

    for (const idx of activePath) {
        // activePath 如 [0, 1] 表示：第 0 列选第 0 项，第 1 列选第 1 项
        if (opts[idx]?.children) {
            // opts[idx] 是当前选中的项
            // opts[idx]?.children 是可选链，如果 children 不存在返回 undefined
            opts = opts[idx].children!;  // !是非空断言，表示"我确定 children 存在"
            result.push(opts);           // 把下一列加入结果
        }
    }
    return result;  // 返回所有列的数组，列数 = activePath.length + 1
});
```

> **什么是 `$derived`？**
> `$derived` 是 Svelte 5 的新响应式 API，相当于 Svelte 4 的 `$:` 推导值。
> - `$derived(expr)`：把表达式的结果变成响应式状态
> - `$derived.by(() => {...})`：当函数内部依赖的状态变化时，自动重新计算

> **什么是 `!` 非空断言？**
> `opts[idx].children!` 中的 `!` 告诉 TypeScript："我保证这个值不是 null 或 undefined，你不用检查了"。
> 这在确认有子节点时才执行这个分支，所以可以安全断言。

#### 3.3.4 交互逻辑

```svelte
// 第 63-71 行：鼠标悬停时的处理
function hoverItem(colIdx: number, itemIdx: number) {
    const item = menus[colIdx][itemIdx];
    // 获取当前 hover 的项

    activePath = activePath.slice(0, colIdx);
    // 把 activePath 截断到当前列
    // 例如 activePath = [0, 1, 2]，hover 第 0 列 → 截断为 []
    // 例如 activePath = [0, 1, 2]，hover 第 1 列 → 截断为 [0]

    if (item.children && item.children.length > 0) {
        // 如果 hover 的项有子节点，把它的索引加入 activePath
        activePath = [...activePath, itemIdx];
        //      ↑ 展开运算符，把数组展开后创建新数组
        //  等价于 activePath = activePath.concat([itemIdx])
    }
    // menus 会自动重新计算，因为 activePath 变了
}
```

> **`.slice(0, colIdx)` 截断逻辑：**
> ```typescript
> activePath = [0, 1, 2]  // 表示：第0列选0，第1列选1，第2列选2
>
> hoverItem(0, 0)  // colIdx=0
> activePath = activePath.slice(0, 0)  // → []（从0截到0，不含index 0）
>
> hoverItem(1, 1)  // colIdx=1
> activePath = activePath.slice(0, 1)  // → [0]（保留index 0）
>
> hoverItem(2, 0)  // colIdx=2
> activePath = activePath.slice(0, 2)  // → [0, 1]（保留index 0,1）
> ```
>
> 这样 hover 从深层回到浅层时，会正确截断 activePath，不会保留深层的信息。

#### 3.3.5 CSS 改造

```css
/* 第 164-177 行 */
.cascader-panel {
    position: absolute;
    top: 100%;
    left: 0;
    margin-top: 0.25rem;
    z-index: 50;
    display: flex;
    background: var(--card-bg, #fff);
    border: 1px solid var(--border, #e5e7eb);
    border-radius: 0.5rem;
    box-shadow: 0 10px 15px -3px rgb(0 0 0 / 0.1);
    overflow: auto;      /* ← 改造：原来是 hidden，改为 auto 允许横向滚动 */
    max-width: 100vw;   /* ← 改造：防止列过多时超出视口 */
}
```

> **`overflow: auto` vs `overflow: hidden`：**
> - `overflow: hidden`：直接裁剪溢出的内容，不显示滚动条
> - `overflow: auto`：如果内容没溢出就正常显示，如果溢出了就显示滚动条
> 这是让级联选择器支持任意多列的关键

### 3.4 `src/components/Navbar.astro` — 导航栏（数据转换层）

#### 3.4.1 文件职责

导航栏负责把从 `getCategoryTree()` 获取的分类树数据，转成 `CascadeSelector` 需要的格式，然后渲染出来。

#### 3.4.2 数据转换函数

```astro
{/*
---
---
*/}
{/* 第 21-33 行 */}
function convertToCascaderOptions(tree: any[], parentPath: string = ""): any[] {
    return tree.map(node => {
        const fullPath = parentPath ? `${parentPath}/${node.name}` : node.name;
        // 累积完整路径：
        //   第一次调用（根级）：parentPath="" → fullPath="编程"
        //   递归调用时：parentPath="编程" → fullPath="编程/前端"

        return {
            value: fullPath,   // 用于跳转 URL
            label: node.name,  // 用于显示文字
            children: node.children && node.children.length > 0
                ? convertToCascaderOptions(node.children, fullPath)
                // ^ 递归调用，fullPath 作为新的 parentPath 传入
                : undefined
                // ^ 没有子节点就返回 undefined，而不是空数组
        };
    });
}
```

> **为什么要递归转换？**
> 因为 `CategoryNode`（content-utils.ts 中的类型）和 `CascadeSelector` 的 `CategoryNode` 结构不完全一样：
> - `CategoryNode.name` → `CascadeSelector.value`（要用完整路径）
> - `CategoryNode.children` → `CascadeSelector.children`（递归映射）
>
> 递归函数负责把整棵树的每个节点都做这种转换。

---

## 四、总结

### 4.1 改造的核心思想

本次改造围绕一个核心问题：**如何让程序自动处理任意层级的分类路径？**

答案是**递归**。无论是数据构建还是 UI 渲染，都用递归代替了原来固定两层的循环。

### 4.2 涉及文件的职责分层

```
展示层（用户看到的）
    ├── Categories.astro     → 侧边栏：递归渲染 HTML 字符串
    └── CascadeSelector.svelte → 导航栏：递归渲染面板列

数据转换层（承上启下）
    └── Navbar.astro         → 把分类树转成级联选择器格式

数据构建层（幕后英雄）
    └── content-utils.ts     → 把文章分类路径解析成树形结构
```

### 4.3 关键语法点汇总

| 语法 | 含义 | 出现位置 |
|---|---|---|
| `parts.slice(1)` | 数组截取，去掉第一个元素 | buildCategoryTree |
| `const [a, ...rest] = arr` | 解构赋值，把第一个元素和剩余元素分开 | buildCategoryTree |
| `\`模板\`` | 模板字符串，嵌入变量 `${变量}` | buildCategoryTree, Categories.astro |
| `node.children?.length` | 可选链，属性不存在时返回 undefined | 到处出现 |
| `!node` | 隐式转换，falsy 值（包括 undefined）时为 true | buildCategoryTree |
| `$derived.by(() => {...})` | Svelte 5 响应式推导，自动重新计算 | CascadeSelector.svelte |
| `activePath.slice(0, colIdx)` | 数组截断到指定位置 | CascadeSelector.svelte |
| `[...arr, item]` | 展开运算符，创建包含新元素的新数组 | CascadeSelector.svelte |
| `set:html={字符串}` | Astro 指令，把字符串作为 HTML 注入 DOM | Categories.astro |
| `type CategoryNode = {...}` | TypeScript 类型别名定义对象结构 | content-utils.ts |
| `.reduce((sum, n) => sum + n, 0)` | 数组归约，把数组求和成一个值 | buildCategoryTree |
| `.localeCompare()` | 按语言习惯比较字符串（支持中文拼音排序） | buildCategoryTree |
